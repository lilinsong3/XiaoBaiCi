package com.github.lilinsong3;

import org.junit.Test;

public class MyFragmentTest {

    @Test
    public void loadFavoriteHanziWord() {
//        try (FragmentScenario<MyFragment> ignored = FragmentScenario.launchInContainer(MyFragment.class)) {
//            onView(withId(R.id.recycler_word)).check((view, noViewFoundException) -> {
//                if (noViewFoundException != null) {
//                    throw noViewFoundException;
//                }
//                RecyclerView recyclerView = (RecyclerView) view;
//                Assert.assertNotNull(recyclerView.getAdapter());
//                assertEquals(0, recyclerView.getAdapter().getItemCount());
//            });
//        }

    }
}
